# 🚀 JobTracker: Система отслеживания вакансий (FastAPI + Telegram Bot)

Краткое описание проекта:

> **JobTracker** — это комплексная система для отслеживания релевантных вакансий с помощью Telegram-бота и API на FastAPI. Приложение позволяет пользователям устанавливать параметры поиска (ключевые слова, регион, минимальная зарплата), сохранять их в базе данных PostgreSQL и получать уведомления о новых, подходящих вакансиях.

---

## 🌟 Основные компоненты

Проект состоит из трех основных частей, запущенных в Docker Compose:

1.  **FastAPI API**: Предоставляет RESTful API для управления пользователями, их параметрами поиска (`/users/{user_id}/params`) и вакансиями. Используется Telegram-ботом и может быть использован внешними сервисами.
2.  **Telegram Bot**: Интерфейс для конечных пользователей. Обрабатывает команды (`/start`, `/setup`, `/list`, `/notifications`), взаимодействует с API и Redis.
3.  **PostgreSQL**: Основная база данных для хранения информации о пользователях и их настройках.
4.  **Redis**: Используется для кэширования

---

## 💻 Технологический стек

-   **Бэкенд (API & Bot):** Python 3.11+
-   **Управление зависимостями:** Poetry
-   **Веб-фреймворк:** FastAPI
-   **База данных:** PostgreSQL
-   **ORM / Асинхронная БД:** SQLAlchemy (Async) / asyncpg
-   **Кэш:** Redis
-   **Миграции:** Alembic
-   **Среда развертывания:** Docker, Docker Compose

---

## 🛠️ Установка и запуск (Docker Compose)

Самый простой способ запустить проект — использовать Docker Compose, который автоматически поднимет все три сервиса (API/Bot, DB, Redis) и настроит их сеть.

### 1. Требования

Для запуска вам потребуются:

-   [Docker Desktop](https://www.docker.com/products/docker-desktop/) (включает Docker и Docker Compose).
-   Git.

### 2. Клонирование репозитория

```bash
git clone [https://github.com/666waffle666/JobTracker-Bot.git]
cd JobTracker
```

### 3. Настройка переменных окружения

Создайте файл **`.env`** в корневой директории проекта и заполните его необходимыми секретами и настройками.

**Файл `.env` (пример):**

```bash
# --- DATABASE CONFIG ---
# Эти переменные используются для инициализации сервиса 'db' и для подключения к нему из сервиса 'app'.
POSTGRES_USER=jobtracker_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=jobtracker_db
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- REDIS CONFIG ---
# Эти переменные используются для подключения к сервису 'redis' из сервиса 'app'.
REDIS_HOST=redis
REDIS_PORT=6379

# --- APPLICATION SECRETS ---
# Токен, необходимый для работы Telegram-бота.
BOT_TOKEN=ВАШ_РЕАЛЬНЫЙ_ТОКЕН_TELEGRAM_БОТА_ЗДЕСЬ
```

### 4. Запуск проекта

Используйте **`Docker Compose`** для сборки и запуска всех сервисов. Команда **`--build`** соберет образ вашего приложения, а затем запустит его, **PostgreSQL** и **Redis**, выполнив автоматические миграции Alembic.

```bash
docker compose up --build -d
```

### 5. Проверка статуса и логов

Проверка контейнеров: Убедитесь, что все контейнеры запущены:

```bash
docker compose ps
```

Просмотр логов: Следите за логами приложения, чтобы убедиться, что миграции прошли успешно и API/Бот запущены:

```bash
docker compose logs -f app
```

Вы должны увидеть сообщение: Alembic migrations applied successfully. ✅.

## ⚙️ Использование

### Доступ к API

После успешного запуска ваш FastAPI API доступен по порту **8000**.

Swagger UI (Интерактивная документация): http://localhost:8000/docs

Redoc: http://localhost:8000/redoc

### Использование Telegram Bot

1. Найдите вашего бота в Telegram.

2. Начните работу с команды /start.

3. Используйте команды для управления параметрами поиска:

    - `/setup` — Установить или изменить параметры поиска.

    - `/list` — Просмотреть текущие активные параметры.

    - `/notifications` — Включить или отключить уведомления.

### 🛑 Остановка и очистка

**Остановка всех сервисов**

Корректная остановка контейнеров:

```bash
docker compose down
```

Удаление данных и томов
Чтобы полностью удалить контейнеры, сети, а также постоянные данные PostgreSQL и Redis (тома):

```bash
docker compose down --volumes
```
