Отлично, Waffle 🐾 — ты уже перешёл к этапу **проектирования функциональности и API**, а это именно то, что превращает идею в рабочую систему.
Давай сделаем полное техническое описание JobTracker, как если бы это был **твой проект в портфолио**:

-   все функции бота,
-   внутренние сервисы,
-   и конкретные эндпоинты FastAPI с методами.

---

## 🧠 Общее описание

**JobTracker** — это Telegram-бот и веб-сервис, который помогает пользователю отслеживать новые вакансии на hh.ru по заданным фильтрам (навыки, город, зарплата, график и т.д.) и получает уведомления в Telegram, когда появляются новые подходящие вакансии.

---

## ⚙️ Основные функциональные возможности

### 1. 👋 Регистрация пользователя

**Описание:**
После команды `/start` бот сохраняет Telegram ID и создаёт профиль пользователя в системе.

**Действия пользователя:**

-   Запускает бота
-   Получает приветствие и предложение настроить фильтры

**API методы:**

```http
POST /users
GET /users/{tg_id}
DELETE /users/{tg_id}
```

**Пример:**

```json
POST /users
{
  "tg_id": 123456789,
  "username": "hellhound_waffle"
}
```

---

### 2. ⚙️ Настройка фильтров поиска вакансий

**Описание:**
Пользователь указывает фильтры (город, ключевые слова, зарплата, тип занятости и др.).
Бот сохраняет эти фильтры для последующих поисков.

**API методы:**

```http
POST /users/{tg_id}/filters
GET /users/{tg_id}/filters
PUT /users/{tg_id}/filters
DELETE /users/{tg_id}/filters
```

**Поля фильтра:**

| Поле              | Тип       | Описание                                             |
| ----------------- | --------- | ---------------------------------------------------- |
| `city`            | str       | Город или ID региона hh.ru                           |
| `keywords`        | list[str] | Список ключевых слов (например, "Python", "FastAPI") |
| `salary_min`      | int       | Минимальная зарплата                                 |
| `employment`      | list[str] | Тип занятости (`full`, `part`, `remote`)             |
| `schedule`        | list[str] | График работы (`remote`, `shift`, `flexible`)        |
| `update_interval` | int       | Интервал обновления в секундах                       |

**Пример:**

```json
POST /users/12345/filters
{
  "city": "Москва",
  "keywords": ["Python", "FastAPI", "PostgreSQL"],
  "salary_min": 80000,
  "employment": ["full", "remote"],
  "schedule": ["remote"],
  "update_interval": 3600
}
```

---

### 3. 🔍 Поиск вакансий

**Описание:**
Сервис делает запрос к **hh.ru API**, используя фильтры пользователя, получает список вакансий и сохраняет новые.

**Внешний API hh.ru:**

```http
GET https://api.hh.ru/vacancies?text=python&area=1&salary=80000
```

**Внутренний API JobTracker:**

```http
GET /jobs/search?user_id={tg_id}
```

**Результат:**

```json
[
    {
        "id": "123456",
        "title": "Python Backend Developer",
        "company": "ООО Технологии Будущего",
        "salary": "от 120000 ₽",
        "url": "https://hh.ru/vacancy/123456",
        "published_at": "2025-11-12T09:15:00"
    }
]
```

---

### 4. 🔔 Уведомления о новых вакансиях

**Описание:**
Планировщик (Celery или APScheduler) периодически проверяет hh.ru для каждого пользователя.
Если найдены новые вакансии, бот отправляет уведомление в Telegram.

**API методы (внутренние):**

```http
POST /notifications/send
```

**Пример уведомления в Telegram:**

```
🔥 Новая вакансия: Python Developer
🏢 Компания: ООО АйТи Решения
💰 Зарплата: от 100 000 ₽
📍 Город: Москва
🔗 https://hh.ru/vacancy/987654
```

---

### 5. ⭐ Избранные вакансии

**Описание:**
Пользователь может сохранить понравившуюся вакансию, чтобы потом вернуться к ней.

**API методы:**

```http
POST /users/{tg_id}/favorites
GET /users/{tg_id}/favorites
DELETE /users/{tg_id}/favorites/{vacancy_id}
```

**Пример:**

```json
POST /users/12345/favorites
{
  "vacancy_id": "123456",
  "title": "Python Backend Developer",
  "url": "https://hh.ru/vacancy/123456"
}
```

---

### 6. 📅 Планировщик задач

**Описание:**

-   Проверяет hh.ru каждые N секунд (у каждого пользователя свой интервал).
-   Вызывает `/jobs/search?user_id={tg_id}`
-   Сравнивает с уже найденными вакансиями (таблица `vacancies_sent`)
-   Если есть новые — отправляет через `/notifications/send`

**API методы (внутренние):**

```http
GET /scheduler/run
```

---

### 7. ⚙️ Административные функции (в будущем)

**Для разработчика / администратора:**

```http
GET /admin/users        # Список пользователей
GET /admin/stats        # Статистика использования
DELETE /admin/user/{id} # Удалить пользователя
```

---

## 🗃️ Структура базы данных (PostgreSQL)

**users**

| Поле       | Тип       | Описание         |
| ---------- | --------- | ---------------- |
| id         | int       | PK               |
| tg_id      | bigint    | Telegram ID      |
| username   | text      | Имя пользователя |
| created_at | timestamp | Дата регистрации |

**filters**

| Поле            | Тип   | Описание                       |
| --------------- | ----- | ------------------------------ |
| id              | int   | PK                             |
| user_id         | int   | FK(users.id)                   |
| city            | text  | Город                          |
| keywords        | jsonb | Список ключевых слов           |
| salary_min      | int   | Минимальная зарплата           |
| employment      | jsonb | Занятость                      |
| schedule        | jsonb | График                         |
| update_interval | int   | Интервал проверки (в секундах) |

**vacancies_sent**

| Поле       | Тип       | Описание                      |
| ---------- | --------- | ----------------------------- |
| id         | int       | PK                            |
| user_id    | int       | FK(users.id)                  |
| vacancy_id | text      | ID вакансии с hh.ru           |
| sent_at    | timestamp | Когда отправлено пользователю |

**favorites**

| Поле       | Тип  | Описание        |
| ---------- | ---- | --------------- |
| id         | int  | PK              |
| user_id    | int  | FK(users.id)    |
| vacancy_id | text | ID вакансии     |
| title      | text | Название        |
| url        | text | Ссылка на hh.ru |

---

## 🧩 Взаимодействие компонентов

1. Пользователь → Telegram Bot → `POST /users`
2. Пользователь → Telegram Bot → `POST /users/{id}/filters`
3. Планировщик → `GET /jobs/search?user_id=...` → hh.ru API
4. FastAPI → Telegram Bot → уведомление
5. Пользователь → Telegram Bot → `/favorites` → `GET /users/{id}/favorites`

---

## 🧠 Итог

JobTracker:

-   Автоматизирует поиск вакансий,
-   Экономит время пользователя,
-   Работает на hh.ru API,
-   Имеет продуманную структуру API для масштабирования и интеграции.

---

Хочешь, я помогу оформить это как полноценный **раздел "Функциональные возможности и API"** в виде Markdown-документа (для README.md или отчёта)?
Там всё будет красиво структурировано с заголовками и примерами запросов.
